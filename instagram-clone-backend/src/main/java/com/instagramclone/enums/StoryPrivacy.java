package com.instagramclone.enums;
public enum StoryPrivacy {
    PUBLIC,        // visible to everyone
    FOLLOWERS,     // only followers can view
    CLOSE_FRIENDS, // only close friends list can view
    PRIVATE        // only the owner can view
}
